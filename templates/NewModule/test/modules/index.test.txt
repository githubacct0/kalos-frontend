/*
    ! Please write your tests here (failing first) and green them as development progresses. 
*/

export {};

import TransactionUserModule = require('../../../modules/TITLE_HERE/main');
import React = require('react');
import Enzyme = require('enzyme');

import Chai = require('chai');

describe('TITLE_HERE', () => {
  describe('<TITLE_HERE userID={100153} />', () => {
    let wrapper: Enzyme.ReactWrapper;
    before(() => {
      wrapper = Enzyme.mount(
        <TransactionUserModule.default userID={8418} />,
      );
    });
    after(() => {
      wrapper.unmount();
    });
    it('renders a loader when loading', () => {
      throw new Error("Testing is not implemented yet for the module TITLE_HERE")
    });
  });
});
