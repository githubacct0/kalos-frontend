import React from 'react';
import ReactDOM from 'react-dom';
import { SpiffToolLogs } from './main';

ReactDOM.render(
  <SpiffToolLogs loggedUserId={101253} />,
  document.getElementById('root'),
);
