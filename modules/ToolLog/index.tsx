import React from 'react';
import ReactDOM from 'react-dom';
import { ToolLog } from './main';

ReactDOM.render(
  <ToolLog loggedUserId={213} userId={103285} withHeader />,
  document.getElementById('root'),
);
