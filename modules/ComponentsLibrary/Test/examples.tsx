import React from 'react';
import { ExampleTitle } from '../helpers';
import { Test } from './index';
export default () => (
  <>
    <ExampleTitle>
      Default (this was to show off unit testing in the frontend)
    </ExampleTitle>
    <Test />
  </>
);
