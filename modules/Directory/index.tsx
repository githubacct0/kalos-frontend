import React from 'react';
import ReactDOM from 'react-dom';
import { Directory } from './main';

ReactDOM.render(
  <Directory userId={8418} withHeader padding={1} />,
  document.getElementById('root'),
);
